package br.com.mob1st.core.state.contracts

/**
 * A standard interface for ViewModels that handle clicks on main button in the UI.
 */
interface PrimaryCtaManager {
    /**
     * Handle a click on main button in the UI.
     */
    fun primaryAction()
}
